⚓ ingester-elasticsearch:
  extends:
    - .autodevops_install
    - .cdtn_base_rules
  image: node:14.16.0-alpine3.11
  cache:
    key:
      files:
        - yarn.lock
      prefix: ${CI_JOB_NAME}
    paths:
      - .cache
  script:
    - yarn global add @socialgouv/yarn-workspace-focus-install
    - yarn exec yarn-workspace-focus-install
      --
      --cwd targets/ingester-elasticsearch
      --
      --frozen-lockfile --prefer-offline
  artifacts:
    expire_in: 1 week
    paths:
      - targets/ingester-elasticsearch/node_modules
      - node_modules

#
#
#

🧪 ingester-elasticsearch:
  extends:
    - .autodevops_test
    - .cdtn_base_rules
  needs:
    - job: ⚓ ingester-elasticsearch
      artifacts: true
  before_script:
    - cd targets/ingester-elasticsearch

🧻 ingester-elasticsearch:
  extends:
    - .autodevops_lint
    - .cdtn_base_rules
  needs:
    - job: ⚓ ingester-elasticsearch
      artifacts: true
  before_script:
    - cd targets/ingester-elasticsearch
  script:
    - yarn types
    - yarn lint

#
#
#

🎯 ingester-elasticsearch:
  extends:
    - .base_yarn_script
    - .cdtn_base_rules
  image: node:14.16.0-alpine3.11
  needs:
    - job: ⚓ ingester-elasticsearch
      artifacts: true
    - job: 📦 elasticsearch-document-adapter
      artifacts: true
    - job: 📦 logger
      artifacts: true
  before_script:
    - cd targets/ingester-elasticsearch
  script:
    - yarn build
  artifacts:
    expire_in: 1 week
    paths:
      - targets/ingester-elasticsearch/bin

#
#
#

🗃 ingester-elasticsearch:
  extends:
    - .autodevops_register_image
    - .cdtn_base_rules
  needs:
    - job: 🎯 ingester-elasticsearch
      artifacts: true
  variables:
    CONTEXT: targets/ingester-elasticsearch
    IMAGE_NAME: $CI_REGISTRY_IMAGE/ingester-elasticsearch

#
#
#

🚀 ingester-elasticsearch (review):
  extends:
    - .base_deploy_kosko_stage
  stage: Deploy
  rules:
    - if: '($CI_PIPELINE_SOURCE == "trigger" || $CI_PIPELINE_SOURCE == "web") && $ACTION == "ingest_documents_dev"'
      when: always
    - if: "$PRODUCTION || $RELEASE || $CI_COMMIT_TAG"
      when: never
    - when: manual
      allow_failure: true
  cache:
    key:
      files:
        - .k8s/yarn.lock
      prefix: k8s
    paths:
      - .cache
      - .k8s/node_modules
  environment:
    name: ${CI_COMMIT_REF_SLUG}-dev2
  needs:
    - job: 🛂 k8s test
    - job: 🗃 ingester-elasticsearch
  dependencies: null
  before_script:
    - kubectl config set-context --current --namespace=${KUBE_NAMESPACE}
    - kubectl delete job ingester-elasticsearch || true
  variables:
    # kosko options
    KOSKO_GENERATE_ARGS: --env dev jobs/ingester-elasticsearch
