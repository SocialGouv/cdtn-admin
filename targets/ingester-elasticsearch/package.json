{
  "name": "ingester-es",
  "description": "SocialGouv - Code du travail numerique - Target - Ingester Elasticsearch",
  "version": "1.7.2",
  "dependencies": {
    "@shared/elasticsearch-document-adapter": "^1.7.2"
  },
  "devDependencies": {
    "@socialgouv/eslint-config-typescript": "^1.70.0",
    "@tsconfig/node14": "^1.0.1",
    "@types/node": "^14.17.4",
    "@vercel/ncc": "^0.28.6",
    "eslint": "^7.23.0",
    "lint-staged": "^11.0.0",
    "prettier": "^2.2.1",
    "typescript": "^4.3.4"
  },
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "build": "ncc build ./src/cli.ts -o ./bin -s",
    "slugchecker-cli": "ncc run ./src/slug-checker.ts",
    "lint": "eslint src",
    "prepush": "yarn build && yarn lint && yarn test",
    "precommit": "lint-staged",
    "start": "node --enable-source-maps --unhandled-rejections=strict bin/index.js",
    "start:dev": "ncc run ./src/cli.ts",
    "test": "yarn types",
    "types": "tsc"
  },
  "lint-staged": {
    "src/**": [
      "yarn lint --cache --fix"
    ]
  }
}
