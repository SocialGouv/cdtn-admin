table:
  name: alert_status
  schema: public
array_relationships:
  - name: alerts
    using:
      foreign_key_constraint_on:
        column: status
        table:
          name: alerts
          schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - name
      filter: {}
      allow_aggregations: true
update_permissions:
  - role: user
    permission:
      columns:
        - name
      filter: {}
      check: {}
