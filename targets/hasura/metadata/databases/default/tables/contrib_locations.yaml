table:
  schema: contrib
  name: locations
array_relationships:
  - name: locations_agreements
    using:
      foreign_key_constraint_on:
        column: location_id
        table:
          schema: contrib
          name: locations_agreements
