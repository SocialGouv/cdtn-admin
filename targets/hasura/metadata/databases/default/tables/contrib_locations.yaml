table:
  name: locations
  schema: contrib
array_relationships:
  - name: locations_agreements
    using:
      foreign_key_constraint_on:
        column: location_id
        table:
          name: locations_agreements
          schema: contrib
