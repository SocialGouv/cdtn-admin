table:
  name: roles
  schema: public
select_permissions:
  - role: super
    permission:
      columns:
        - role
      filter: {}
      allow_aggregations: true
  - role: user
    permission:
      columns:
        - role
      filter: {}
