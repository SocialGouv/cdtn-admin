table:
  name: agreements
  schema: public
select_permissions:
  - role: super
    permission:
      columns:
        - id
        - kali_id
        - name
      filter: {}
      allow_aggregations: true
