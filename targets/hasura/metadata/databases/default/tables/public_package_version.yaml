table:
  name: package_version
  schema: public
insert_permissions:
  - role: super
    permission:
      check: {}
      columns:
        - repository
        - version
        - created_at
        - updated_at
select_permissions:
  - role: public
    permission:
      columns:
        - repository
        - version
        - created_at
        - updated_at
      filter: {}
  - role: super
    permission:
      columns:
        - repository
        - version
        - created_at
        - updated_at
      filter: {}
      allow_aggregations: true
  - role: user
    permission:
      columns:
        - repository
        - version
        - created_at
        - updated_at
      filter: {}
update_permissions:
  - role: super
    permission:
      columns:
        - repository
        - version
        - created_at
        - updated_at
      filter: {}
      check: {}
delete_permissions:
  - role: super
    permission:
      backend_only: false
      filter: {}
