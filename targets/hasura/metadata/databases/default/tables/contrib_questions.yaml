table:
  name: questions
  schema: contrib
array_relationships:
  - name: answers
    using:
      foreign_key_constraint_on:
        column: question_id
        table:
          name: answers
          schema: contrib
