table:
  schema: public
  name: sources
array_relationships:
  - name: alerts
    using:
      foreign_key_constraint_on:
        column: repository
        table:
          schema: public
          name: alerts
select_permissions:
  - role: user
    permission:
      columns:
        - repository
        - label
        - tag
        - created_at
      filter: {}
      allow_aggregations: true
update_permissions:
  - role: user
    permission:
      columns:
        - repository
        - label
        - tag
        - created_at
      filter: {}
      check: {}
