table:
  name: sources
  schema: public
array_relationships:
  - name: alerts
    using:
      foreign_key_constraint_on:
        column: repository
        table:
          name: alerts
          schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - repository
        - label
        - tag
        - created_at
      filter: {}
      allow_aggregations: true
update_permissions:
  - role: user
    permission:
      columns:
        - repository
        - label
        - tag
        - created_at
      filter: {}
      check: {}
