🗃 hasura:
  extends:
    - .autodevops_register_image
    - .cdtn_base_rules
  variables:
    CONTEXT: ./targets/hasura
    IMAGE_NAME: $CI_REGISTRY_IMAGE/hasura

#
#
#

# todo: restrict to branches ?
⏪ Restore data:
  extends:
    - .base_deploy_kosko_stage
    - .cdtn_base_rules
  stage: Deploy
  environment:
    name: ${AUTO_DEVOPS_PROD_ENVIRONMENT_NAME}
  needs:
    - job: 🛂 k8s test
  variables:
    KOSKO_GENERATE_ARGS: --env dev jobs/restore
  before_script:
    - echo "kubectl config set-context --current --namespace=${K8S_NAMESPACE}"
    - kubectl get job.batch/restore-db-alpha || exit 0

#
#
#

🚧 Cleanup db cronjob (prod):
  extends:
    - .base_deploy_kosko_stage
    - .cdtn_base_rules
  rules:
    - if: "$PRODUCTION && $CI_COMMIT_TAG"
      when: always
  environment:
    name: ${AUTO_DEVOPS_PROD_ENVIRONMENT_NAME}
  variables:
    KOSKO_GENERATE_ARGS: --env prod jobs/cleanup-db
