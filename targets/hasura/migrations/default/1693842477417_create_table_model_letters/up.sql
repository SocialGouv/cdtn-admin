CREATE TABLE "model"."models" ("id" uuid NOT NULL DEFAULT gen_random_uuid(), "created_at" timestamptz NOT NULL DEFAULT now(), "updated_at" timestamptz NOT NULL DEFAULT now(), "title" text NOT NULL, "description" text NOT NULL, "type" text NOT NULL, "file_name" Text NOT NULL, "file_size" integer NOT NULL, "preview_html" text NOT NULL, PRIMARY KEY ("id") , UNIQUE ("id"));COMMENT ON TABLE "model"."models" IS E'Contient l\'ensemble des modèles de lettre du site';
