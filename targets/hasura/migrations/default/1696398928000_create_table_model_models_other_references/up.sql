CREATE TABLE "model"."models_other_references" ("id" uuid NOT NULL DEFAULT gen_random_uuid(), "model_id" uuid NOT NULL, "label" text NOT NULL, "url" text, PRIMARY KEY ("id") , FOREIGN KEY ("model_id") REFERENCES "model"."models"("id") ON UPDATE cascade ON DELETE cascade, UNIQUE ("id"));COMMENT ON TABLE "model"."models_other_references" IS E'Autres références (saisie à la main) liées aux modèles';
