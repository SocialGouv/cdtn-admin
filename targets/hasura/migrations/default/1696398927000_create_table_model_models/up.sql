CREATE TABLE "model"."models" ("id" uuid NOT NULL DEFAULT gen_random_uuid(), "created_at" timestamptz NOT NULL DEFAULT now(), "updated_at" timestamptz NOT NULL DEFAULT now(), "title" text NOT NULL, "description" text NOT NULL, "meta_title" text NOT NULL, "meta_description" text NOT NULL, "type" text NOT NULL, "preview_html" text NOT NULL, "file_id" uuid NOT NULL, PRIMARY KEY ("id") , UNIQUE ("id"), FOREIGN KEY ("file_id") REFERENCES "public"."files"("id") ON UPDATE cascade ON DELETE cascade);COMMENT ON TABLE "model"."models" IS E'Contient l\'ensemble des modèles de lettre du site';
