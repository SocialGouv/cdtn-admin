{
  "name": "@shared/dila-resolver",
  "version": "1.8.11",
  "main": "lib/index.js",
  "files": [
    "lib"
  ],
  "repository": {
    "directory": "shared/dila-resolver",
    "type": "git",
    "url": "https://github.com/SocialGouv/cdtn-admin.git"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@socialgouv/dila-api-client": "^1.2.0"
  },
  "devDependencies": {
    "@babel/core": "^7.15.5",
    "@babel/plugin-transform-modules-commonjs": "^7.15.0",
    "@babel/preset-typescript": "^7.15.0",
    "@shared/types": "^1.8.11",
    "@socialgouv/eslint-config-typescript": "^1.88.0",
    "@tsconfig/node14": "^1.0.1",
    "@types/jest": "^26.0.24",
    "eslint": "^7.32.0",
    "jest": "^27.0.6",
    "lint-staged": "^11.1.2",
    "prettier": "^2.3.2",
    "ts-node": "^10.2.1",
    "typescript": "^4.3.5"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "ts-node src/main.ts",
    "lint": "eslint . --cache",
    "lint:fix": "yarn lint --fix",
    "precommit": "lint-staged",
    "prepush": "yarn build && yarn lint && yarn test --bail --changedSince=master",
    "test": "jest"
  },
  "lint-staged": {
    "*.{j,t}s": [
      "eslint --cache --fix",
      "jest --bail --findRelatedTests"
    ]
  },
  "babel": {
    "env": {
      "test": {
        "presets": [
          "@babel/preset-typescript"
        ],
        "plugins": [
          "@babel/plugin-transform-modules-commonjs"
        ]
      }
    }
  }
}
