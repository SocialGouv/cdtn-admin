{
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^9.0.1",
    "@commitlint/config-conventional": "^8.3.4",
    "lerna": "^3.22.1",
    "nps": "^5.9.12",
    "nps-utils": "^1.7.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "nps pre-commit",
      "pre-push": " nps pre-push",
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  },
  "private": true,
  "scripts": {
    "build": "nps build",
    "dev": "nps dev",
    "lint": "nps lint",
    "start": "nps",
    "test": "nps test"
  },
  "workspaces": [
    "shared/*",
    "targets/*"
  ],
  "name": "cdtn-admin"
}
