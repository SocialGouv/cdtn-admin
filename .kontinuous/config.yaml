projectName: cdtn
dependencies:
  fabrique:
    extends:
      - name: buildkit-service
    dependencies:
      contrib:
        preDeploy:
          importSecrets:
            options:
              secrets:
                cdtnadminprodserver-backup-credentials:
                  env: [ dev, preprod ]
                  required: true
